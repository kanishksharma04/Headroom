{
  "$schema": "https://openapi.vercel.sh/vercel.json",
  "framework": "nextjs",
  "regions": ["bom1"],
  "buildCommand": "prisma migrate deploy && next build",
  "crons": [{ "path": "/api/cron/attention-digest", "schedule": "30 0 * * *" }]
}
